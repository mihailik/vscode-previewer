{
  "name": "web-previewer",
  "displayName": "web-previewer",
  "description": "",
  "version": "0.9.0",
  "publisher": "mihailik",
  "engines": {
    "vscode": "^1.73.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:web-previewer.activeDocumentAsHtml",
    "onCommand:web-previewer.selectedDocumentAsHtml"
  ],
  "browser": "./index.js",
  "contributes": {
    "commands": [
      {
        "command": "web-previewer.activeDocumentAsHtml",
        "title": "🌍 HTML Preview"
      },
      {
        "command": "web-previewer.selectedDocumentAsHtml",
        "title": "🌍 HTML Preview"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "web-previewer.selectedDocumentAsHtml",
          "when": "resourceExtname == .html || resourceExtname == .htm"
        }
      ],
      "editor/context": [
        {
          "command": "web-previewer.selectedDocumentAsHtml",
          "when": "editorLangId == html"
        }
      ],
      "editor/title": [
        {
          "command": "web-previewer.selectedDocumentAsHtml",
          "when": "resourceExtname == .html || resourceExtname == .htm"
        }
      ],
      "editor/title/context": [
        {
          "command": "web-previewer.selectedDocumentAsHtml",
          "when": "resourceExtname == .html || resourceExtname == .htm"
        }
      ],
      "commandPalette": [
        {
          "command": "web-previewer.activeDocumentAsHtml",
          "when": "editorLangId == html"
        }
      ]
    }
  },
  "scripts": {
    "start": "vscode-test-web --browserType=chromium --extensionDevelopmentPath=. .",
    "vsce_package": "vsce package",
    "vsce_publish": "vsce publish"
  },
  "devDependencies": {
    "@types/vscode": "^1.73.0",
    "@vscode/test-web": "^0.0.32",
    "vsce": "^2.14.0"
  }
}
